services:
  dbservice:
    image: mysql:8.0
    container_name: jobportaldb
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: jobportal
    volumes:
      - ./data/docker:/var/lib/mysql
